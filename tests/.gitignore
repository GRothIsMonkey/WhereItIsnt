# Reproducible from git, and large: `git show <ref>:game.html > baseline.html`.
baseline.html
node_modules/
package-lock.json
# Regenerated by `node render-journey.js`.
renders/
# The Phase 20 build, for measuring what the journey REVISION cost on top of it:
#   git show b2bc032:game.html > phase20.html
phase20.html
# The pre-Phase-21 build, for the item-contact before/after comparison:
#   git show 71ec935:game.html > phase20_2.html
phase20_2.html
# Vendored three.js r128 for the hermetic browser run. Reproducible with:
#   curl -o tests/vendor/three.min.js https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js
vendor/
